<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - MatchUp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .profile-container {
            max-width: 800px;
            margin: 30px auto;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .profile-header {
            background-color: #4CAF50;
            color: white;
            padding: 30px;
            text-align: center;
        }
        .profile-avatar {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            border: 5px solid white;
            margin: 0 auto 15px;
            background-color: #e9ecef;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 50px;
        }
        .profile-content {
            padding: 30px;
        }
        .stats-card {
            background-color: #f8f9fa;
            border-radius: 10px;
            padding: 15px;
            margin-bottom: 20px;
        }
        .social-links a {
            color: #4CAF50;
            margin-right: 15px;
            font-size: 24px;
        }
        .level-badge {
            background-color: #4CAF50;
        }
        .position-badge {
            background-color: #2196F3;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="profile-container">
            <div class="profile-header">
                <div class="profile-avatar">
                    <i class="fa fa-user"></i>
                </div>
                <h2><%= user.username %></h2>
                <p class="mb-0"><%= user.email %></p>
            </div>

            <div class="profile-content">
                <div class="row mb-4">
                    <div class="col-md-6">
                        <h4>Player Information</h4>
                        <div class="mb-3">
                            <span class="badge level-badge"><%= user.level %></span>
                            <span class="badge position-badge"><%= user.position %></span>
                        </div>
                        <p>Member since: <%= new Date(user.createdAt).toLocaleDateString() %></p>
                    </div>
                    
                    <div class="col-md-6">
                        <h4>Statistics</h4>
                        <div class="stats-card">
                            <div class="row text-center">
                                <div class="col-4">
                                    <h5><%= user.stats.matchesPlayed %></h5>
                                    <p class="text-muted">Matches</p>
                                </div>
                                <div class="col-4">
                                    <h5><%= user.stats.goals %></h5>
                                    <p class="text-muted">Goals</p>
                                </div>
                                <div class="col-4">
                                    <h5><%= user.stats.assists %></h5>
                                    <p class="text-muted">Assists</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <hr>
                
                <div class="row">
                    <div class="col-12">
                        <h4>Contact Information</h4>
                        <div class="social-links mt-3">
                            <% if(user.socialLinks.instagram) { %>
                                <a href="https://instagram.com/<%= user.socialLinks.instagram %>" target="_blank">
                                    <i class="fab fa-instagram"></i>
                                </a>
                            <% } %>
                            
                            <% if(user.socialLinks.snapchat) { %>
                                <a href="https://snapchat.com/add/<%= user.socialLinks.snapchat %>" target="_blank">
                                    <i class="fab fa-snapchat"></i>
                                </a>
                            <% } %>
                            
                            <% if(user.socialLinks.whatsapp) { %>
                                <a href="https://wa.me/<%= user.socialLinks.whatsapp %>" target="_blank">
                                    <i class="fab fa-whatsapp"></i>
                                </a>
                            <% } %>
                            
                            <% if(user.socialLinks.phone) { %>
                                <a href="tel:<%= user.socialLinks.phone %>">
                                    <i class="fas fa-phone"></i>
                                </a>
                            <% } %>
                        </div>
                    </div>
                </div>   
                <div class="text-center mt-4">
                    <a href="/edit-profile" class="btn btn-outline-success">Edit Profile</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js" integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO" crossorigin="anonymous"></script>
</body>
</html>